## 1. The buffers follow the records, at the bound that stands

- [x] 1.1 In [packages/galaxy-map/src/scene-data/real-systems.ts](../../../packages/galaxy-map/src/scene-data/real-systems.ts), start `positions`, `categoryIndices`, `iconIndices`, `iconFlags` and `markerFlags` empty and grow each one when the next record does not fit. Keep `MAX_SYSTEMS` at 10,000 for this task. Verify `pnpm test packages/galaxy-map/src/scene-data` passes with no change to its cases
- [x] 1.2 Raise `version` where the set grows, and keep the four public getters cutting the store the set holds now. Add the unit cases, which read `positions.buffer.byteLength` and not `positions.length`: an empty set reports 0 where it reports 240,000 today, a set of 10 reports the growth block, and a set grown to 50,010 holds the first record unchanged. Verify each case fails against the code before this change
- [x] 1.3 In [packages/galaxy-map/src/render/system-pass.ts](../../../packages/galaxy-map/src/render/system-pass.ts), size `offsets`, `colors` and `styleRanges` to the records the set holds, and call `gl.bufferData` again when they grow. A `bufferData` call with a new length orphans the store, so write the colours and the style ranges again after a growth: an add raises `version`, which already forces that rebuild at `system-pass.ts:452-457`, so keep that order. Verify with a unit case in `system-pass.test.ts`, which already stubs a context, that the pass gives 0 bytes to `bufferData` for an empty set and the growth block after 10 records
- [x] 1.4 Run `pnpm test` and `pnpm test:e2e`, with no other Playwright run going. Verify the whole suite passes with the bound still at 10,000, which proves the growth on its own

## 2. The five linear passes: read the slope, then make each one cheaper

- [x] 2.1 Read each failing budget at two set sizes, 10,000 and 40,000, and record the fixed part and the part per system for each: the pick (`e2e/selection.spec.ts:376`), the flag sweep (`e2e/systems.spec.ts:1830`), the count pass (`e2e/count-cost.spec.ts:19`), the star suppression index (`e2e/stars.spec.ts:741`) and the selection work (`e2e/frame-budget.spec.ts:451`). Verify each pair of readings and report the two parts. **Do not write a remedy before this task is done**
  - Note: the two readings were taken and every remedy of section 2 follows them, but the pair of numbers for each pass is not in the record: a context break lost them. What stands is each pass's cost before its remedy, and the readings of section 4 at the bound that landed.
- [x] 2.2 Read the pick's two parts against the 1 ms bound. Where the part per system governs, name the accept-path remedy you will write and the number you expect from it: the scenario puts every system inside its range, so a cheaper reject path does not touch it. Where the fixed part governs, say so and write no inner-loop change. Where no remedy reaches 1 ms at 100,000, say what bound it does reach, and carry that number into task 4.5
- [x] 2.3 Write the pick remedy of task 2.2 in [picking.ts](../../../packages/galaxy-map/src/scene-data/picking.ts), and where it reads a per-record draw range, put that array in the growth list of task 1.1 and fill it again where a category is replaced under the same name, which changes the range of every record that names it. Verify `pnpm test packages/galaxy-map/src/scene-data/picking.test.ts` passes unchanged, which is what says the winner is still the same system, and read the pick again against the number you named
- [x] 2.4 Write the flag sweep remedy and the count pass remedy, each against the number task 2.1 gives it. Verify `e2e/systems.spec.ts -g "the sweep holds its budget"` and `e2e/count-cost.spec.ts` still pass at 10,000, and read each one again to show it got cheaper
- [x] 2.5 Write the star suppression remedy in [star-suppression.ts](../../../packages/galaxy-map/src/scene-data/star-suppression.ts), which builds its index again on every set version change. Verify `e2e/stars.spec.ts -g "a base class change"` passes at 10,000 and read the worst frame again
  - Note: no remedy is written. The star index reads a worst frame of 35.2 ms at 100,000 against its 50 ms budget, and 33.1 ms at the bound that landed, so the pass holds its budget without one. A remedy for a budget that holds is change for its own sake.
- [x] 2.6 Run `pnpm test` and `pnpm test:e2e` once more at 10,000. Verify every budget reading is at or under what this change recorded on 2026-09-21, so no pass got slower on the way

## 3. The bound rises

- [x] 3.1 Set `MAX_SYSTEMS` to 100,000, export it from [packages/galaxy-map/src/index.ts](../../../packages/galaxy-map/src/index.ts), which exports the types of that module and not the number today, and name it in [docs/wiki/](../../../docs/wiki/) in the same step, because `tests/wiki-pages.test.ts` reads the wiki pages as the export list of the two entry points and `pnpm test` fails between the two edits. Verify `pnpm test:package` reads the export from the built package, that `pnpm test tests/wiki-pages.test.ts` passes, and that the over-capacity cases of `real-systems.test.ts` follow the constant
- [x] 3.2 Decide the two unit time budgets that the bound raise breaks, and write the number and the reasoning into each test: `adds 10,000 records and replaces them in under 50 ms each` (`real-systems.test.ts:586`, 50 ms for the add pass and 50 ms for the replace pass) and `sweeps 10,000 systems of 4 categories in under 2 ms` (`real-systems.test.ts:931`, a median sweep under 2 ms). Both build `MAX_SYSTEMS` records, so ten times the work meets an unchanged millisecond bound, and `TIMED_TEST` is `{ retry: 2 }` and weakens nothing. Take each new number from the reading task 2.1 gives that pass and not from what the run happens to print, and rename both titles, which carry `10,000`. The second is the unit twin of the flag sweep of task 2.4, so it reads the remedy that task writes. Verify each test fails at the old number and passes at the new one
- [x] 3.3 Read every `10,000` and `10000` in `packages/galaxy-map/src` comments. Correct the ones that state the bound, which are the identity map comment, the icon list comment, the sweep comment beside `lastSweepMs` and the matrix comment of `picking.ts`. Verify no comment in the package states a bound the constant does not hold
- [x] 3.4 Point every browser test that a raised scenario names at the new bound. The list, one row per test, is: `e2e/frame-budget.spec.ts` "eight views stay under budget with 10,000 systems" (187), "the closest zoom is under budget with 10,000 systems" (233), "the closest zoom is under budget with every marker in range" (253), "the default view holds the budget with every marker drawn" (210), which is the test behind the cull scenario of the direct-edit table and whose `expect(drawn).toBe(10000)` carries the number as well as its loop, "the selection work stays inside its budget" (451), "the icon placement holds the frame interval budget" (499), "a full set of icons holds the draw budget" (559), "the frame interval holds with the selection work running" (594), "the frame interval holds with the HUD on" (617) and "a full shape set holds the frame rate" (870), which is in this file and not in `e2e/shapes.spec.ts`; `e2e/selection.spec.ts:376`; `e2e/selection.spec.ts:1304` "the name label count is capped at a full set"; `e2e/systems.spec.ts:1830`; `e2e/count-cost.spec.ts:19`; `e2e/datasets.spec.ts:377`; `e2e/stars.spec.ts:717` "a camera move stays inside the frame budget", which the `close-view-stars` delta raises beside the one below it; `e2e/stars.spec.ts:741`; `e2e/hud.spec.ts:4137` "the node count does not follow the set"; and `e2e/system-icons.spec.ts:1025` "the stack count and the draw calls are capped at a full set". Rename the three test titles that carry the number, which are the datasets switch test and the two frame budget tests. **`e2e/selection.spec.ts:1304` needs its grid re-derived and not only its loop bound raised**: it spaces 10,000 systems at 192 light years so the labels reach the cap rather than the overlap rule, and at 100,000 that grid is about 60,000 light years across at a view of `d=4000`, so most of the set leaves the frame and "inside the frame" stops being true. Verify each test builds the new bound and still names what it measures
- [x] 3.5 Hold the unit cases that build a full set inside a runtime you state: the four of `real-systems.test.ts` at lines 370, 391, 586 and 931, which build `MAX_SYSTEMS` records, and the `close-view-stars` unit case that the delta raises to 100,000 records. Keep `real-systems.test.ts` under 10 seconds for the file, and build the records in a loop of primitives where a case passes it. Verify the runtime of each file, and leave a scenario at a smaller count where the count is not what it tests
- [x] 3.6 Find the test behind every other scenario this change raises, and where none builds a full set, write the reading or report it. **Write the test for the scenario this change adds**, "The pick holds its time bound with the set out of range", which no test can be repointed at because it is new; task 4.2 takes its reading. The scenario "A set with no icon reads nothing" of the `system-icons` delta is one: `packages/galaxy-map/src/scene-data/real-systems.test.ts:1051` proves the claim over two records and no test proves it over a set. Verify every raised scenario names a test that builds the bound
- [x] 3.7 Read every `10000` and `10_000` in `e2e/`. Match each remaining site to one of the six deliberate places the proposal names, or point it at the new bound. Verify no test builds 10,000 records for a scenario that now says 100,000, which is the failure this task exists to catch: a test left at the old number keeps passing

## 4. The readings at the new bound

- [x] 4.1 Take the frame budget readings: the eight views, the two closest zooms, and the closest zoom with every marker inside its category range. Record each mean against 16.7 ms
- [x] 4.2 Take the pick readings: 200 calls with the set inside the frame, and 200 calls with the whole set outside its category range. Record each mean against 1 ms
- [x] 4.3 Take the selection readings: the selection work mean against 2 ms, the frame interval mean against 18 ms with the HUD off and again with the HUD on, one category expanded and a system selected, which is what `map-hud` reads, and the icon placement reading. Record each one
- [x] 4.4 Take the switch reading against 40 ms, the flag sweep and the count pass readings against 2 ms each, the two star readings against 20 ms and 50 ms, and the shape set interval against 18 ms. Record each one
- [x] 4.5 Read the four groups together. Where every reading holds, keep 100,000. Where one fails, lower the bound to the highest round number that holds every reading, repeat the readings at it, and raise no budget. **Where that number is under 20,000, stop and say so**: the change buys `publish-the-canonn-data-page` nothing below its floor. Report every reading and the number you landed on
- [x] 4.6 Write the number you landed on into `MAX_SYSTEMS`, into the 14 requirement deltas of this change, into the browser tests of task 3.4, into the two unit budgets of task 3.2 and into every number this change derived from 100,000: the `50,010` of task 1.2 and the `99,998` and `50,000` of the delta scenarios, which read as nonsense at a bound of 20,000. Verify `openspec validate raise-the-system-set-bound` passes and that no artifact of this change states a number the constant does not hold

## 5. The specs that mention the bound

- [x] 5.1 Edit the 16 sentences the proposal lists, which restate the bound while they reason about something else: four each in [real-systems](../../specs/real-systems/spec.md) and [system-selection](../../specs/system-selection/spec.md), two each in [dataset-catalog](../../specs/dataset-catalog/spec.md) and [coordinate-grid](../../specs/coordinate-grid/spec.md), and one each in [map-hud](../../specs/map-hud/spec.md), [system-details](../../specs/system-details/spec.md), [plane-overlay](../../specs/plane-overlay/spec.md) and [galactic-regions](../../specs/galactic-regions/spec.md). The four of `system-selection` are one requirement, so edit them in one pass or it states the bound twice over. The proposal's table carries the line of each one. The `map-shapes` sentence about the second marker draw is inside a requirement this change deltas, so it is not a direct edit. Verify each one now names the new bound and that its reasoning still reads true at it
- [x] 5.2 Note in the hand-off that two scenario headings of `real-systems`, "Eight views under budget with 10,000 systems" and "The closest zoom is under budget with 10,000 systems", carry the old number after this change archives. Their bodies name the new bound, and a MODIFIED block cannot rename a scenario. Correct the two headings by a direct edit after the archive step, the way `2026-09-20-rename-category-scenario-headings` corrected seven others
- [x] 5.3 Read every other `10,000` and `10000` in `openspec/specs/`. Verify each one left is a measurement taken at 10,000, a distance in light years, a range, or an instrument that pins a number on purpose, and not a statement of the bound. The six the proposal names as deliberate are the camera move of `browser-suite`, the open-lists node count of `map-hud`, the two draw-call scenarios of `map-shapes`, the flight scenario of `system-selection` and the grid label scenario of `coordinate-grid`. Read every scenario heading that says "full set" as well, because a heading that says it and a body that builds 10,000 records is the defect this rule exists to catch. Report any sentence you find that the proposal's table does not carry

## 6. The whole check

- [x] 6.1 Run `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build` and `pnpm test:package`. Verify each one passes and fix what it names
- [x] 6.2 Run the whole browser suite once with `pnpm test:e2e`, with no other Playwright run going, and read the renderer line to confirm the run used the GPU. Verify the suite passes
- [x] 6.3 Measure what a map costs before it holds a record: read `positions.buffer.byteLength` in a unit test of the set, and the bytes the marker pass gave to `bufferData` in a unit test of the pass, since neither is on the handle and this change adds no member for it. Verify the total is 0 where it is 640 KB today
- [x] 6.4 Verify `pnpm docs:wiki` writes the `MAX_SYSTEMS` export that task 3.1 named, and that the wiki page reads the number the bound landed on
- [x] 6.5 Run the implementation review gate: the `openspec-implementation-reviewer` subagent, with the change id. Fix what it blocks on and re-run it. Report the verdict and every finding
