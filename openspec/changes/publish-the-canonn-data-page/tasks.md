## 1. The two changes this one waits for

- [ ] 1.1 Confirm `publish-the-sample-pages` is archived, so `apps/demo/vite.config.ts` already takes many build inputs. Stop and say so if it is not
- [ ] 1.2 Confirm `raise-the-system-set-bound` is archived, and read the bound it landed from `MAX_SYSTEMS` and from `openspec/specs/real-systems/spec.md`. Where it is under 100,000, read the split rule of `specs/canonn-data-page/spec.md` again and write the entry count the splits give at that number into this change. Stop and say so if it is not archived
- [ ] 1.3 Put the committed size in front of the project owner before any reader is written: about 41 MB of minified JSON under a 50 MB ceiling, on top of the 25 MB ceiling `publish-the-sample-pages` holds and the 980 KB the tree holds today, which is 75 MB where both land at their ceilings. State the ceilings and not the estimates. Give them the table of what each map costs, so they can hold a map back and not only answer yes or no: Cloud fetches 32.49 MB, All 21.73 MB and landscape 6.66 MB, which is 61 MB of the 67.5 MB the build reads, and the codex sets are 22.9 MB of the 41 MB. Say that the estimate spans 37 MB to 50 MB, because one ratio per source shape is measured on one file of that shape. Wait for the answer. Where the answer is to hold maps back, mark those maps in the group table of task 2.1 and write what you held back into the proposal

## 2. The map table and the source readers

- [ ] 2.1 Write the map table of `apps/demo/scripts/build-canonn-sets.mjs`: one row per `MapData-*.js` file, with the identifier, the label, the subject group, the source shape, the addresses that shape needs, **what its reader does to make a key** and the colour it gives each key, which are the addresses the map's own file names on `storage.googleapis.com/canonn-downloads`. Mark the six rows the demo page already converts, so a reader of the table sees the overlap. Verify the table holds 49 rows and that each row names a file of the Canonn source tree
- [ ] 2.2 Make the build list the Canonn source tree from the GitHub contents API at run time, and fail, naming the file, on a `MapData-*.js` file the table does not name or on a listing it cannot read. Verify with a unit test over a committed listing fixture that holds one unknown file, which reads the failure path and not the listing
- [ ] 2.3 Write the `literal` reader over `parseEd3dData`, `ed3dCategories` and `ed3dRecords`, which `build-demo-systems.mjs` exports. Verify it converts the five maps that hold static records against a fixture, with no network
- [ ] 2.4 Write the `dump` reader for the JSON files of the `canonn-downloads` bucket, taking each map's key from that map's own reader and not from a column. `MapData-Cloud.js:380-407` is the case that needs the table: it keys on the `description` column, renames `Storm Cloud` to `Lagrange Cloud`, and for a row outside `Lagrange Cloud` and `FSS Signals` puts the `category` value in the key and calls the group `Contents`, so 24 column values become 35 keys. Verify it converts the beacons, megaships, generation ships and IDA fixtures, that one row per site becomes one record per system, and that the Clouds fixture gives 35 keys and not 24
- [ ] 2.5 Record which map's reader keyed each file, and fail, naming both maps and the file, where a second map names that source and keys it differently. Verify with a two-map fixture that the build fails and names both
- [ ] 2.6 Write a stable colour for every key: the map's own palette read in source order where the map has one, and a colour of the build's own where the map builds its table with `randomColor()`, which 17 of the 49 files do. Verify a second run writes the same colours
- [ ] 2.7 Write the `codexCsv` reader for `canonn-downloads/dumpr/<subject>/<id>.csv`, with the codex ids read from the map's own file. The colours and the category names are manifest data, so the reader does not read them. Give a codex record no description of its own, because the category of a codex set is the codex entry and a description per record repeats it on every line and adds about 10 MB to the tree. Verify it converts a Biology fixture and a Thargoid fixture, that no converted record carries a description, and that a subject whose columns do not match is named and left out
- [ ] 2.8 Write the `query` reader for the Canonn cloud function endpoints. Verify it converts the NHSS systems fixture, and that an endpoint that answers with an error is named and left out
- [ ] 2.9 Read `MAX_SYSTEMS` from the built library package, which `raise-the-system-set-bound` exports, and fail where the build cannot read it. Verify the build names the export and does not fall back to a number of its own
- [ ] 2.10 Write the file table: one committed file per converted source, holding records and no naming, and one manifest entry per map naming the files its map reads, each with a table from the category keys of that file to the category name and the colour that map gives each key. Allow one path to appear twice in one entry under two categories, which `MapData-All.js` does with `dumpr/Thargoid/2100101.csv`. Verify with a unit test that two maps naming one source name one file, that each of the two carries its own category name and colour, that the build writes no source to two paths, and that the twice-named file is fetched once and added under both categories. `MapData-All.js` names 60 of the 63 codex files, so this is what holds the tree at its union and not at 62 MB
- [ ] 2.11 Write the split pass, against **both** bounds: a map over the record bound **or over the 16 MB per-set ceiling** splits by its categories, a category still over a bound splits into numbered parts that are inside both bounds, every part carries the map's group, every part's `id` is the map id, the category and the part number joined by hyphens, and the pass prints what it split. Read the record bound from `MAX_SYSTEMS` and the byte ceiling from one place both this pass and the size test of task 4.5 read. Make the build fail, naming the count and the 256 bound, where the splits make more entries than the catalog reads. Verify with three fixtures: one of 3 times the record bound, one inside the record bound and over the byte ceiling, and one whose splits pass the catalog bound. In the first two, verify that no record is lost, that every part is inside both bounds and that no two ids repeat

## 3. The data

- [ ] 3.1 Write the entry part: fetch into the ignored `apps/demo/data/canonn/`, write one minified file per converted source to `apps/demo/demo-data/canonn/`, each record holding the system, its coordinates, its keys and the source descriptions that are not one of its own keys, and no category name, colour or map-composed text, write `index.json` with one row per entry and, in each row, the path, the category name and the colour of every file that entry reads, and print every map it dropped with the address, every split it made, the total it wrote and the largest set. Add `build:canonn-data` to `apps/demo/package.json` and the root entry that delegates to it. Verify `pnpm test tests/workflow.test.ts` passes
- [ ] 3.2 Make the build fail, naming the map, the address, the count it read and the committed count, where a map the committed tree already holds gives no record or gives 10 percent fewer records or worse. Verify with two fixtures: a source that answers with nothing, and a source that answers with 80 percent of its rows
- [ ] 3.3 Run the build over every source. Report the dropped maps, the splits, the total size and the largest set. Where the measured total passes **50 MB**, which is the ceiling and not the estimate, stop, tell the project owner the real number with the per-map table of task 1.3, and wait, before you commit. A tree over 50 MB fails the size test either way, so the ceiling is the number that starts the conversation. Verify a second run writes the same bytes
- [ ] 3.4 Commit the sets and the manifest. Verify the tree is under the 50 MB ceiling and every set under 16 MB. Report the measured total beside both numbers, and leave the ceiling where it is: a lower ceiling is a later change, not an edit of the requirement while you implement it
- [ ] 3.5 Add `apps/demo/demo-data/canonn/` to [.prettierignore](../../../.prettierignore), beside the committed sets it already names, so `pnpm format` does not expand the minified sets and make the tree dirty at the next data build. Verify `pnpm format` leaves the directory alone

## 4. The page

- [ ] 4.1 Write `apps/demo/canonn/index.html`, `main.ts` and the worker that parses a set, following `apps/demo/src/multifaction.ts` and its worker: read `index.json`, build one catalog entry per row with its group, fetch the files the picked row names, parse them off the main thread and join their records into one set. Open the page on one of the sets under 1 MB, not on the largest, as the demo page opens on a set that is not `multifaction`. Add the page to the build inputs of `apps/demo/vite.config.ts` and `apps/demo/canonn` to the `include` list of [tsconfig.json](../../../tsconfig.json), which would otherwise type check no file of the page. Verify the dev server draws the page, the dialog groups the entries, and `pnpm exec tsc --noEmit` reads the page
- [ ] 4.2 Measure the worker hand-back beside the parse on the largest set, because `addSystems` takes record objects and the main thread pays a structured clone of up to 71,142 of them. Report both times. Where the clone is the larger of the two, post packed typed arrays as transferables and build the records in slices on the main thread. Verify the reading is written into the change and that the page loads the largest set either way
- [ ] 4.3 Keep every set a file: set `build.assetsInlineLimit` to 0 in `apps/demo/vite.config.ts`, or put the sets where Vite copies them as they stand, because Vite inlines an asset under 4,096 bytes as a data URL and several of the static-record maps are that small. Verify the built tree holds the smallest set as a file and that no chunk carries its records
- [ ] 4.4 Widen the `no-restricted-imports` scope of [eslint.config.js](../../../eslint.config.js) to `apps/demo/canonn/**`, as `publish-the-sample-pages` widens it to the other page directories, and add the case to `tests/lint-config.test.ts`. Verify the case fails without the rule
- [ ] 4.5 Add `tests/canonn-page.test.ts`: every manifest row names files that exist, every file is named by a row, no source is committed twice, each `systemCount` is the record count of the row's files together, no set passes the system bound, each set carries a source and a licence line, the catalog stays inside 256 entries, and the two size ceilings hold. Verify `pnpm test tests/canonn-page.test.ts` passes
- [ ] 4.6 Add `e2e/canonn-page.spec.ts`: the page opens on a set under 1 MB and fetches no larger one, the catalog holds the manifest's entries, one set loads with the categories its manifest row names and with no `over-capacity` rejection, the page fetched the files that entry names and no others, **a multi-file entry loads as well**, which `Aliens` is with its 16 sources and which is the path no other test covers, and two entries that read one shared file each show their own map's category name and colour, and the mean frame interval from the fetch to the moment the records reach `loadDataset` is 18 ms or less, which is what says the parse ran in the worker. Read the window to that moment and not past it: `dataset-catalog` gives the `loadDataset` call itself 40 ms, which is two to three dropped frames by its own words, and that cost is not this page's. Verify with a targeted Playwright run
- [ ] 4.7 Load the largest set in the browser and read the frame statistics, the selection work reading and the frame interval. Report each one against the budget `raise-the-system-set-bound` holds at the set bound: 16.7 ms of draw, 2 ms of selection work and an 18 ms frame interval. Report the transfer size and the time from the pick to the first frame that holds the records

## 5. Notices, documentation and the whole check

- [ ] 5.1 Add one section per new Canonn source to [THIRD_PARTY_NOTICES.md](../../../THIRD_PARTY_NOTICES.md): the downloads bucket, the codex CSV dumps and the query endpoints, each saying what the conversion takes. Verify `pnpm test tests/third-party-notices.test.ts` passes
- [ ] 5.2 Name the page in [README.md](../../../README.md) and in the wiki Overview page, link it from the demo page, and correct the script counts from six and seven to seven and seven in both [AGENTS.md](../../../AGENTS.md) and [openspec/config.yaml](../../config.yaml), which carries the same sentence as the project context. `publish-the-sample-pages` takes them from five and seven to six and seven, so read what is there before you edit. Verify the links open on the built site
- [ ] 5.3 Run `pnpm lint`, `pnpm exec tsc --noEmit`, `pnpm test`, `pnpm build`, `pnpm build:demo-site` and `pnpm test:package`. Verify each one passes and fix what it names
- [ ] 5.4 Run the whole browser suite once with `pnpm test:e2e`, with no other Playwright run going, and read the renderer line to confirm the run used the GPU. Verify the suite passes
- [ ] 5.5 Run the implementation review gate: the `openspec-implementation-reviewer` subagent, with the change id. Fix what it blocks on and re-run it. Report the verdict and every finding
