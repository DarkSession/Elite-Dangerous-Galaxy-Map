## 1. Tests first

- [x] 1.1 In `packages/galaxy-map/src/app/create-map.test.ts`, replace "holds the view at the frame distance and frames it below" with "holds the view below the frame distance". Set the camera at `FIT - 1` and at 20 light years on the origin, load `second`, and assert `getView()` is equal before and after for both. Verify: `pnpm test` fails on this test before task 2.1.
- [x] 1.2 In `e2e/datasets.spec.ts`, replace "a camera zoomed in closer than the frame is framed again" with "a camera zoomed in closer than the frame keeps its view". Keep the `tiny` start entry. Assert `before.distance < FIT_DISTANCE` and `after` equals `before`. Verify: the test fails before task 2.1.
- [x] 1.3 In `e2e/datasets.spec.ts`, add "a camera on one system keeps its view" to the same `describe`. Load `week-one`, set the cursor to the corner `[100, 50, 100]` of `BOX`, a distance of 20, a yaw of 25 and a pitch of 40, load `week-two`, and assert the view is equal before and after. Verify: the test fails before task 2.1.
- [x] 1.4 In `e2e/cycles-page.spec.ts`, replace "a cycle wider than the view is framed again" with "an untouched camera is held over a wider cycle". Read the view, click **next dataset**, wait for the counter, and read the view again. Assert the two readings are equal. Assert `before.distance` is under the frame distance of the second cycle, which is `2 * R` for the box of `boxOf(SECOND)`, as the `FIT_DISTANCE` of `e2e/datasets.spec.ts` reads it. Verify: the test fails before task 2.1.
- [x] 1.5 In `e2e/cycles-page.spec.ts`, add "stepping to the next cycle holds a camera on one system". Take the coordinates of the first record of the first cycle from its committed file. Call `setView` with that cursor, a distance of 20, a yaw of 90 and a pitch of 20. Click **next dataset**, wait for the counter, and assert the view is equal before and after and `systemCount()` is `SECOND.count`. Verify: the test fails before task 2.1.
- [x] 1.6 In `e2e/cycles-page.spec.ts`, delete "opens the camera on the box of the cycle it loads", as design D4 states. Verify: `grep -n "opens the camera on the box" e2e/cycles-page.spec.ts` finds nothing.

## 2. The rule

- [x] 2.1 In `viewInsideBounds` of `packages/galaxy-map/src/app/create-map.ts`, delete the `systemsFitDistance()` test and its comment. Keep `systemsFitDistance`, and delete from its doc comment the clause that `viewInsideBounds` reads it. Change the comment on the empty-box test to the reason in design D2. Verify: the tests of tasks 1.1 to 1.5 pass, and "applies the view over an empty set under a sphere bound" still passes.

## 3. Comments and documentation

- [x] 3.1 In `packages/galaxy-map/src/app/datasets.ts`, change the `DatasetView` comment: its lead sentence says a camera inside the new bounds keeps its place, it names four conditions, and item 5 goes. Change the `DatasetEntry.view` comment to "whenever the camera is inside the new bounds" and "four conditions". Change the comment above `held` in `runLoad` the same way. Verify: `pnpm lint` passes.
- [x] 3.2 Change "all five conditions" to "all four conditions" in `packages/galaxy-map/src/app/datasets.test.ts` and in the start-load test of `e2e/datasets.spec.ts`. Rename the test "holds the view where the camera already shows the new set" of `datasets.test.ts` to "holds the view where the camera is inside the new bounds". Rename the `describe` "a load holds a camera that shows the new set" of `e2e/datasets.spec.ts` to "a load holds a camera inside the new bounds", and change its comment to "the four conditions". Verify: the grep of task 3.5 finds none in these files.
- [x] 3.3 In `apps/demo/cycles/main.ts`, `apps/demo/canonn/main.ts` and the Thargoid war entry of `apps/demo/src/main.ts`, change the comments to say a load holds a camera inside the new bounds, and to name four conditions. Change comments only. `apps/demo/canonn/main.ts` and `apps/demo/src/main.ts` have uncommitted edits of the owner. Before the edit, save `git diff` of the two files to the scratchpad. Verify: in a diff of the saved output against `git diff` after the edit, every added or deleted line is a comment line. A changed `@@` header or moved context line is expected, because the comment in `apps/demo/src/main.ts` sits next to the owner's hunk.
- [x] 3.4 In `docs/wiki/Examples/A-dataset-catalog.md`, change the bold lead "A camera that already shows the new set keeps its place" to "A camera inside the bounds of the new set keeps its place". Delete "and the camera stands at least as far out as the frame would put it" from the list of conditions. Add one sentence: a camera zoomed in on one system keeps its place. Verify: `pnpm docs:wiki` builds.
- [x] 3.5 Change the comments of `e2e/cycles-page.spec.ts` that name condition 5 or "five conditions", and the requirement name "A step between cycles holds the camera", to the new rule and the new name. Verify: `grep -rn -i "five conditions\|condition 5\|fifth condition\|already shows\? \(the new\|that cycle\|it\b\)" packages/galaxy-map/src apps/demo/src apps/demo/cycles apps/demo/canonn e2e docs` finds nothing. The main specs under `openspec/specs/` still name the old rule until the archive merges the deltas; after `/opsx:archive`, the same grep over `openspec/specs` finds nothing.

## 4. Check the whole tree

- [x] 4.1 Run `pnpm lint`, `pnpm test` and `openspec validate hold-camera-inside-bounds --strict`. Verify: all three pass.
- [x] 4.2 After `pnpm test` stops, run `pnpm build:demo-site`, then run the two changed browser files: `GALAXY_MAP_E2E_BUILT=1 pnpm exec playwright test --project=chromium-gpu e2e/datasets.spec.ts e2e/cycles-page.spec.ts`. The build comes first, because the flag serves the last build and task 2.1 changes the source. Run no other Playwright run at the same time. Verify: every test passes on a hardware renderer.
- [x] 4.3 Run `pnpm test:e2e` once for the full suite. Verify: it passes. If a test fails outside the two files of task 4.2, report its output.
- [x] 4.4 Run the implementation review gate (`openspec-implementation-reviewer`) and fix each BLOCK finding. Verify: the verdict is APPROVE or APPROVE WITH NOTES.
