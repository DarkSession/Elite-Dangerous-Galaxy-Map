# CLAUDE.md

This project keeps its agent instructions in one place, for every tool rather than just
this one. Read them there:

@AGENTS.md

The `@` above is an import: Claude Code pulls the contents of `AGENTS.md` into context
along with this file, so edit `AGENTS.md` and leave this pointer alone.
